name: conan-package

on:
  push:
    paths:
      - 'src/**'
      - 'include/**'
      - 'apps/**'
      - 'conanfile.py'
      - 'conandata.yml'
      - 'CMakeLists.txt'
      - '.github/workflows/package.yml'
    branches:
      - main
      - master
      - 'UC-*'
      - 'NP-*'
      - 'CURA-*'
      - 'PP-*'
      - '[0-9].[0-9]*'
      - '[0-9].[0-9][0-9]*'
    tags:
      - '[0-9].[0-9].[0-9]*'
      - '[0-9].[0-9][0-9].[0-9]*'

jobs:
  conan-package:
    uses: ultimaker/cura-workflows/.github/workflows/conan-package.yml@main
    secrets: inherit
